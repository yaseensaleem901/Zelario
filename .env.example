# Demo mode (default): mock API — no backend required; user login needs a real wallet extension
NEXT_PUBLIC_DEMO_MODE=true

# SPA mode (default): client-only app shell, no server cookies/middleware
NEXT_PUBLIC_SPA_MODE=true

# Set to false only if you run the legacy Express backend
# NEXT_PUBLIC_API_URL=http://localhost:5000
