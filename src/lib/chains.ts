import { sepolia } from 'wagmi/chains';

/** Default chain for on-chain features (NFT marketplace, DEX, points claim). */
export const activeChain = sepolia;

export const activeChainId = sepolia.id;
